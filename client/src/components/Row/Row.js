import React from "react";

function Row({ fields }) {
  // console.log(fields);
  return (
    <tr>
      {/* {fields.map((value) => {
        return <td>{value}</td>;
      })} */}
    </tr>
  );
}

export default Row;
